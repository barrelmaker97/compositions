---
services:
  node-exporter:
    container_name: node-exporter
    image: prom/node-exporter:v1.9.1
    restart: unless-stopped
    network_mode: host
    pid: host
    volumes:
      - "/:/host:ro"
    command:
      - "--path.rootfs=/host"
